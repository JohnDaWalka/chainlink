#!/usr/bin/env bash

set -e
CONTRACT=$1
SRC=src/v0.8/"$CONTRACT".sol
echo "Compiling $SRC with zksolc"

../tools/bin/zksolc -O3 \
    --bin --metadata-literal --metadata-hash none \
    --evm-version paris \
    -o zksolc/"$CONTRACT" --overwrite \
    $SRC
